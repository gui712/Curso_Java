package aplication;

public class Program {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/* 
		 * Lista é uma estrutura de dados
		 * Homogenea dados do mesmo tipo
		 * Ordenada acessada por meio de posições
		 * inicia vazia, e seus elementos são alocados sob demanda
		 * cada elemento ocupa um nó ou nodo da lista
		 * Tipo(interface): List não pode instanciar um tipo List pq ela é a interface
		 * classes que implementam a interface List: ArrayList
		 * 
		 * vantagens 
		 * tamanho variavel # do vetor que tem que criar o tamanho
		 * facilidade de realizar inserções e deleções
		 * 
		 * desvantagens
		 * Acesse sequencia aos elementos
		 * dependendo da implementação a navegação pode ser otimizada
		 * 
		 * Tamanho da lista: size()
		 * inserir elemento na ista: add(obj), add(,int,obj)
		 * Remover elementos da lista: remove(obj), remove(int), removeIf(Predicate)
		 * Encontrar posição de elemento: indexOf(obj), lastIndexOf(obj)
		 * Filtrar lista com base em predicado:
		 * List<Integer> result = list.stream().filter(x -> x > 4).collect(Collectors.toList());
		 * Encontrar primeira ocorrencia com base em predicado:
		 * Integer result = list.stream().filter(x -> x >4).findFirst().orElse(null);
		 */

	}

}
